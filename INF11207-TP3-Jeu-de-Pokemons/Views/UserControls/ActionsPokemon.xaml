<UserControl x:Class="INF11207_TP3_Jeu_de_Pokemons.Views.UserControls.ActionsPokemon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid
        Margin="10,0,10,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button
            Content="Échanger"
            Background="Gray"
            Command="{Binding CommandeEchanger}"/>

        <Button
            Content="{Binding ContentButtonAction}"
            Grid.Row="1"
            Background="Gray"
            Command="{Binding CommandeAction}"/>
    </Grid>
</UserControl>
